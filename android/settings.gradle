pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }

  def reactNativeGradlePlugin = new File(
    providers.exec {
      workingDir(rootDir)
      commandLine("node", "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
    }.standardOutput.asText.get().trim()
  ).getParentFile().absolutePath
  includeBuild(reactNativeGradlePlugin)
  
  def expoPluginsPath = new File(
    providers.exec {
      workingDir(rootDir)
      commandLine("node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })")
    }.standardOutput.asText.get().trim(),
    "../android/expo-gradle-plugin"
  ).absolutePath
  includeBuild(expoPluginsPath)
}

plugins {
  id("expo-autolinking-settings")
  id("com.facebook.react.settings")
}

expoAutolinking.useExpoModules()

// Generate autolinking.json and link native RN libraries (required by @react-native/gradle-plugin 0.74+)
// expo-autolinking-settings handles expo modules; this handles traditional RN packages.
reactSettings.autolinkLibrariesFromCommand(expoAutolinking.rnConfigCommand)

rootProject.name = 'Word of the Day'

expoAutolinking.useExpoVersionCatalog()

include ':app'
includeBuild(expoAutolinking.reactNativeGradlePlugin)
